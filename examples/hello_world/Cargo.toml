[package]
name = "hello-world"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
rovo = { path = "../.." }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
